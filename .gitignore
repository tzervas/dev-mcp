
!.env.example
!.vscode/mcp.json
# (this repo aggregates by link/submodule reference, not by vendoring source)
# Editors
# Env / secrets — never commit these
# If servers are ever vendored as submodule checkouts locally
# Logs / scratch
# OS cruft
*.key
*.log
*.pem
.DS_Store
.direnv/
.env
.env.*
.idea/
.venv-check/
.vscode/*
Thumbs.db
