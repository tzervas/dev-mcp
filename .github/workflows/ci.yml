name: CI

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  check:
    name: Local-parity checks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Local-parity checks
        run: bash scripts/check.sh
